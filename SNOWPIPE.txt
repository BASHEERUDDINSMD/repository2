 SNOWPIPE:
 DATALOADING USING WEB/BROWSER AND EXTERNAL STAGES OR LOCATION ARE MANUAL LOADING
 SNOWPIPE AUTOMATION PROCESS 
 
 create snowpipe for table 
 event notification in aws properties
 
 
 DATA LOADING TYPES:
    using WEB UI/wizard/browser
	using EXTERNAL STAGE
	using EXTERNAL LOCATION
	SNOWPIPE 
	STREAMS & TASK 
	
SHOW PIPES;
USE ROLE ACCOUNTADMIN;
ALTER PIPE TEST_PIPE SET PIPE_EXECUTION_PAUSED=true;
GRANT OWNERSHIP ON PIPE "HYD_STG"."DATA_LOADS"."TEST_PIPE" to role "SYSADMIN";
ALTER PIPE TEST_PIPE SET PIPE_EXECUTION_PAUSED=FALSE;
SELECT SYSTEM$PIPE_FORCE_RESUME('TEST_PIPE');
SHOW STAGES;
SELECT SYSTEM$PIPE_STATUS('TEST_PIPE');
LIST @SHAIK_SAMPLE_STAGE;

select * from table(information_schema.copy_history(table_name=>'"HYD_STG"."DATA_LOADS"."CATALOG_STG"', start_time=> dateadd(hours, -3, current_timestamp())));
SELECT * FROM TABLE(VALIDATE_PIPE_LOAD(PIPE_NAME=>'"HYD_STG"."DATA_LOADS"."TEST_PIPE"',START_TIME=>DATEADD(HOUR,-3,CURRENT_TIMESTAMP())));
